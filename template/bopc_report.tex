\documentclass[a4paper,%
11pt,%
DIV12,
headsepline,%
headings=normal,
]{scrartcl}

\usepackage{bopc}
\usepackage{csvsimple}
\usepackage{graphicx}
%% ==================== ADAPT ACCORDINGLY ==================== %%

% args: first name, last name, matriculation number
\setAuthorOne{Max}{Spannring}{12104980}
\setAuthorTwo{Ida}{Neuhauser}{11806595}

% set group size and group number, group size influences author formatting

% args: group size, group number (TUWEL)
%\setGroup{2}{X}
\setGroup{2}{1}

%% =========================================================== %%

\begin{document}

\maketitlepage

%% ================= SUBMISSION INFORMATION ================== %%

% Please fill out accordingly

%% ========================= REPORT ========================== %%

\setcounter{section}{1}
\section{The Tasks}

\setcounter{subsection}{1}
\subsection{Compute Speed-up and Parallel Efficiency for 2 Instance Sizes }

\def\sizes{155}
\def\sizel{1100}

\begin{table}[htb]
 \centering 
\caption{\label{tab:runtime_cs}Runtime and speed-up of parallel Julia set generator for $c_s$ case.}
\begin{tabular}{rrrrr}
  \toprule
  size & p & mean runtime (s) & speed-up & par. eff.\\
  \midrule
  \sizes & 1 & 4.3 & fill & fill \\
    & $\vdots$ &  & .. & .. \\
  \sizes & 32 & 5.1 &  \\
  \sizel & 1 & 6.3 \\
    & $\vdots$ &  \\
  \sizel & 32 & 6.1 \\
  \bottomrule
\end{tabular}
\end{table}

\begin{table}[htb]
 \centering 
\caption{\label{tab:runtime_cb}Runtime and speed-up of parallel Julia set generator for $c_b$ case.}
\begin{tabular}{rrrrr}
  \toprule
  size & p & mean runtime (s) & speed-up & par. eff.\\
  \midrule
  \sizes & 1 & 4.3 & fill & fill \\
    & $\vdots$ &  & .. & .. \\
  \sizes & 32 & 5.1 &  \\
  \sizel & 1 & 6.3 \\
    & $\vdots$ &  \\
  \sizel & 32 & 6.1 \\
  \bottomrule
\end{tabular}
\end{table}

Tables~\ref{tab:runtime_cs} and \ref{tab:runtime_cb} present the parallel runtime measurements and the respective speed-up and efficiency values for the cases $c_s$ and $c_b$.


\subsection{Influence of Patch Size}

\subsection{Finding the Best Patch Size}

\section{Speed-up Analysis}

Two algorithms $A_1, A_2$ are given with the following parallel runtimes:
\[T_{par}^{A_1}(n,p) \in O(\frac{n \log n}{p} + \log^2(n)),\]
\[T_{par}^{A_2}(n,p) \in O(\frac{n \log n}{p} + 3\cdot n.\]

The sequential time for both is defined as $T_{seq}^*(n) \in O(n\log n)$.\\

The \textit{absolute speed-up $S_a$} is given by $S_a(n,p)= \frac{T_{seq}(n)}{T_{par}(n,p)}.$

For algorithm $A_1$, this gives:\\
\[S_a^{A_1}(n,p) = \frac{n \log(n)}{\frac{n \log n	}{p}+\log^2 n}.\]

As for algorithm $A_2$, this gives:\\
\[S_a^{A_2}(n,p) = \frac{n \log(n)}{\frac{n \log n	}{p}+3n}.\]

Table \ref{speedup_table} shows the different \textit{absolute speed-ups} for $n=500, p =\lbrace 4,16,64 \rbrace$.


		
\begin{table}[htb]
	\centering 
	\caption{\label{speedup_table}Absolute speed-up of algorithms with fixed n = 500 by number of cores.}
	\begin{tabular}{rrrr}
		\toprule
		 p & 4 & 16 & 64 \\
		\midrule
		$S_a^{A_1}$ & 3.81 & 13.35 & 35.65  \\
		$S_a^{A_2}$ & 1.36 & 1.83 & 2.00  \\
		\bottomrule
	\end{tabular}
\end{table}

When comparing both algorithms with the ideal speed-up, $A_1$ performs nicely enough with 4 cores, moderately with 16 and bad with 64 cores. $A_2$ does not offer benefits of parallelization with any amount of cores. Figure \ref{figure:speed-up} shows the relationship between ideal and absolute speed-ups. 

\begin{figure}[h]
	\centering
	\includegraphics[width=0.5\linewidth]{figs/speed-up-figure}
	\caption{Absolute Speed-up by cores and algorithms.}
	\label{figure:speed-up}
\end{figure}


 
The absolute speed-up alone does not quantify the gain from parallelizing a code. If we look at the parallel efficiency is given by $E(n,p) = \frac{T_{seq(n)}}{p\cdot T_{par}(n,p)} = \frac{S_a(n,p)}{p}$, we can see that for both algorithms, the efficiency drops off quite quickly (see Figure \ref{figure:efficiency}). Table \ref{efficiency_table} shows the parallel efficiency of the algorithms. Both do not scale well. 



\begin{table}[htb]
	\centering 
	\caption{\label{efficiency_table}Parallel efficiency of algorithms with fixed n = 500 by number of cores.}
	\begin{tabular}{rrrr}
		\toprule
		p & 4 & 16 & 64 \\
		\midrule
		$S_a^{A_1}$ & 0.95 & 0.83 & 0.56  \\
		$S_a^{A_2}$ & 0.34 & 0.11 & 0.03  \\
		\bottomrule
	\end{tabular}
\end{table}

\begin{figure}[h]
	\centering
	\includegraphics[width=0.5\linewidth]{figs/efficiency-figure}
	\caption{Absolute Speed-up by cores and algorithms.}
	\label{figure:efficiency}
\end{figure}

The potential speed-up is determined by the sequential fraction of a code. \\

For $A_1$, the sequential fraction $s_{A1}$ is given by:\\

\[s_{A1}(n) = \frac{\log^2(n)}{n\log n} = \frac{\log n}{n}.\]\\

The potential speed-up for algorithm 1 is then given by\\
\[S^{A_1}(n,p) = \frac{1}{s_{A1} + \frac{1-s_{A1}}{p}} = \frac{1}{\frac{\log n}{n} + \frac{1-\frac{\log n}{n}}{p}}\leq \frac{n}{\log n} = S^{A_1}(n).\]\\


Regarding $A_2$, $s_{A2}$ is given by:\\
\[s_{A2}(n) = \frac{3\cdot n}{n\log n} = \frac{3}{\log n}.\]\\

The potential speed-up for algorithm 2 is then given by\\
\[S^{A_2}(n,p) = \frac{1}{s_{A2} + \frac{1-s_{A2}}{p}} = \frac{1}{\frac{3}{\log n} + \frac{1-\frac{3}{\log n}}{p}}\leq \frac{\log n}{3} = S^{A_2}(n).\]\\


\section{Weak Scaling Analysis}

\end{document}
